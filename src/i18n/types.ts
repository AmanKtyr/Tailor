export type SupportedLocale = 'en' | 'es' | 'zh' | 'ja' | 'de' | 'hi';

export interface TranslationDictionary {
  tagline: string;
  initSuccess: string;
  specCreated: string;
  planGenerated: string;
  tasksGenerated: string;
  syncSuccess: string;
  securityPass: string;
  securityFail: string;
  reuseFound: string;
  doctorHealthy: string;
}
