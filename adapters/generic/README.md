# Generic Agent Adapter

The generic adapter supports standard agent setups using:
- `.ai/INDEX.md` as the main project memory entrypoint.
- `AGENTS.md` at the repository root as the high-level contract.
- Standard `skills/` directory adhering to the open agent skills convention.
